/**
 * In Next files we will see use of Context patter.
 * This is useful when there exists a tree of nested components and need to pass values
 * which are needed by many deep level components. In a traditional way value will flow through
 * props, but there will be so many intermediate components. We need an effective way.
 */